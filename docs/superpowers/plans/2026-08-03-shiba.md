# Shiba en el loft — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Perro Shiba animado que camina por el loft con actividad de scroll y salta al clickearlo.

**Architecture:** Módulo autocontenido `dog.js` (carga, mixer, waypoints) orquestado por Scene.vue; raycast de click vía `addExtraTarget` en interactions.js. Spec: `docs/superpowers/specs/2026-08-03-shiba-design.md`.

**Tech Stack:** three (GLTFLoader/AnimationMixer), gltf-transform (dev).

## Global Constraints

- JavaScript only; solo `Scene.vue` y `utils/three/` importan three.
- Commits aprobados por el usuario (solo locales; push solo a pedido). Mensajes terminan con `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Los nombres de nodos del GLB llegan sanitizados por GLTFLoader (ver notas as-built del spec de fase 1).
- No romper: guard `disposed`, bounds de cámara, flujo POI (guard re-entrada + blur del previo), cleanup en unmount.
- La verificación visual/funcional en browser la hace el controlador (tab MCP hidden: sin rAF; el controlador fuerza renders — re-exponer `window.__loft` dev-only en Task 2 y quitarlo al cerrar el plan).

---

### Task 1: Pipeline del Shiba

**Files:** Move: `Shiba Inu by Quaternius - y4wdQpg767.glb` → `model-source/shiba.glb` · Modify: `package.json` · Create: `public/models/shiba.glb`

**Interfaces:** Produces `public/models/shiba.glb` (< 900 KB) con las animaciones `Idle`, `Idle_2`, `Walk`, `Gallop_Jump` presentes.

- [ ] **Step 1:** `mv "Shiba Inu by Quaternius - y4wdQpg767.glb" model-source/shiba.glb`
- [ ] **Step 2:** Agregar script a package.json: `"optimize-dog": "gltf-transform optimize model-source/shiba.glb public/models/shiba.glb --compress draco --texture-compress webp --join false"`
- [ ] **Step 3:** `npm run optimize-dog` y verificar: `npx gltf-transform inspect public/models/shiba.glb` lista las 4 animaciones necesarias; tamaño < 900 KB.
- [ ] **Step 4:** Commit: `feat: pipeline del shiba (GLB optimizado con animaciones)`

---

### Task 2: Módulo dog.js + integración base (perro en idle)

**Files:** Create: `app/utils/three/dog.js` · Modify: `app/components/three/Scene.vue`

**Interfaces:**
- Produces: `createDog({ THREE, scene, floorY, waypoints }) => Promise<{ object, update(delta), setWalking(bool), jump(), dispose() }>`. `object` es el `THREE.Group` raíz del perro (para raycasting). `update` avanza mixer y movimiento. Task 3 usa `setWalking`; Task 4 usa `jump` y `object`.

- [ ] **Step 1:** Implementar `app/utils/three/dog.js`:

```js
import { GLTFLoader } from 'three/examples/jsm/loaders/GLTFLoader.js'
import { DRACOLoader } from 'three/examples/jsm/loaders/DRACOLoader.js'

const TARGET_HEIGHT = 0.5
const WALK_SPEED = 0.9
const TURN_SPEED = 6

export async function createDog ({ THREE, scene, floorY, waypoints }) {
  const draco = new DRACOLoader()
  draco.setDecoderPath('/draco/')
  const loader = new GLTFLoader()
  loader.setDRACOLoader(draco)
  const gltf = await loader.loadAsync('/models/shiba.glb')
  draco.dispose()

  const object = gltf.scene
  const box = new THREE.Box3().setFromObject(object)
  const scale = TARGET_HEIGHT / box.getSize(new THREE.Vector3()).y
  object.scale.setScalar(scale)
  object.position.set(waypoints[0][0], floorY, waypoints[0][1])
  scene.add(object)

  const mixer = new THREE.AnimationMixer(object)
  const clip = name => gltf.animations.find(a => a.name === name)
  const actions = {
    idle: mixer.clipAction(clip('Idle')),
    walk: mixer.clipAction(clip('Walk')),
    jump: mixer.clipAction(clip('Gallop_Jump'))
  }
  actions.jump.setLoop(THREE.LoopOnce)
  actions.idle.play()

  let current = 'idle'
  let jumping = false
  let wpIndex = 1
  const dir = new THREE.Vector3()

  function crossfade (name, dur = 0.3) {
    if (current === name) return
    actions[name].reset().fadeIn(dur).play()
    actions[current].fadeOut(dur)
    current = name
  }

  mixer.addEventListener('finished', (e) => {
    if (e.action !== actions.jump) return
    jumping = false
    actions[current].reset().fadeIn(0.25).play()
    actions.jump.fadeOut(0.25)
  })

  return {
    object,
    setWalking (v) {
      if (jumping) return
      crossfade(v ? 'walk' : 'idle')
    },
    jump () {
      if (jumping) return
      jumping = true
      actions[current].fadeOut(0.15)
      actions.jump.reset().fadeIn(0.15).play()
    },
    update (delta) {
      mixer.update(delta)
      if (current !== 'walk' || jumping) return
      const [tx, tz] = waypoints[wpIndex]
      dir.set(tx - object.position.x, 0, tz - object.position.z)
      const dist = dir.length()
      if (dist < 0.15) { wpIndex = (wpIndex + 1) % waypoints.length; return }
      dir.normalize()
      const targetAngle = Math.atan2(dir.x, dir.z)
      let d = targetAngle - object.rotation.y
      while (d > Math.PI) d -= Math.PI * 2
      while (d < -Math.PI) d += Math.PI * 2
      object.rotation.y += THREE.MathUtils.clamp(d, -TURN_SPEED * delta, TURN_SPEED * delta)
      object.position.addScaledVector(dir, WALK_SPEED * delta)
    },
    dispose () {
      mixer.stopAllAction()
      object.removeFromParent()
    }
  }
}
```

- [ ] **Step 2:** En `Scene.vue`: importar `createDog`; declarar `let dog = null` y `const WAYPOINTS = [[2.5, 2.2], [6.0, 2.0], [6.2, -1.5], [2.5, -1.8]]`. Tras crear `interactions` (y con `bounds` ya computado), cargar el perro sin bloquear la escena:

```js
const floorTopY = bounds ? bounds.min.y + 0.01 : 0.92
createDog({ THREE: ctx.THREE, scene: ctx.scene, floorY: floorTopY, waypoints: WAYPOINTS })
  .then((d) => {
    if (disposed) { d.dispose(); return }
    dog = d
    ctx.addTick(delta => dog.update(delta))
  })
  .catch(err => console.warn('No se pudo cargar el shiba', err))
```

En `onUnmounted`, `dog?.dispose()` antes de `ctx?.dispose()`. Re-exponer `if (import.meta.dev) { window.__loft = { ctx, model, rig, interactions, getDog: () => dog } }` (se elimina al cerrar el plan).

- [ ] **Step 3:** Verificar: `npm test` verde; dev server + curl 200; visual (controlador): perro en idle sobre el piso a escala creíble.
- [ ] **Step 4:** Commit: `feat: shiba con mixer e idle en la escena`

---

### Task 3: Scroll → caminata por waypoints

**Files:** Modify: `app/components/three/Scene.vue`

- [ ] **Step 1:** En `onMounted`, tras crear interactions:

```js
let lastScroll = 0
const onWheel = () => { lastScroll = performance.now() }
ctx.renderer.domElement.addEventListener('wheel', onWheel, { passive: true })
ctx.addTick(() => { dog?.setWalking(performance.now() - lastScroll < 600) })
```

En `onUnmounted`, remover el listener `wheel`.

- [ ] **Step 2:** Verificar (controlador): con eventos de rueda el perro camina el recorrido rotando hacia su dirección; sin rueda vuelve a idle; el zoom de cámara sigue funcionando.
- [ ] **Step 3:** Commit: `feat: el shiba camina con actividad de scroll`

---

### Task 4: Click → salto (extraTargets en interactions)

**Files:** Modify: `app/utils/three/interactions.js`, `app/components/three/Scene.vue`

**Interfaces:** Produces `interactions.addExtraTarget(object3d, onClick) => void`.

- [ ] **Step 1:** En `interactions.js`: mantener un array `extras = []` (`{ object, onClick }`). `addExtraTarget(object, onClick)` lo agrega. En `pick(e)`: raycastear `[...interactive]` (no recursivo, como hoy) y además cada `extras[i].object` con `intersectObject(obj, true)`; quedarse con el hit de menor `distance` global. Devolver `{ mesh, extra }` según el ganador. Ajustar `onMove` (cursor pointer también para extras, highlight solo POIs) y `onUp` (extra ganador → `extra.onClick()`; mesh POI → `onPoiClick`; nada → `onMissClick?.()`). Exponer `addExtraTarget` en el objeto retornado y limpiar `extras` en `dispose()`.
- [ ] **Step 2:** En `Scene.vue`, dentro del `.then` del perro: `interactions.addExtraTarget(dog.object, () => dog.jump())`.
- [ ] **Step 3:** Verificar: `npm test` verde; visual (controlador): click sobre el perro → salta y retoma; click durante salto no hace nada; POIs y click-afuera siguen funcionando.
- [ ] **Step 4:** Commit: `feat: el shiba salta al clickearlo`

---

### Cierre del plan

- [ ] Quitar `window.__loft` de `Scene.vue`; `npm test` + `npm run build` verdes; commit `chore: cierre shiba fase 2a`. Review final de la feature (diff completo del plan) antes del commit de cierre.
