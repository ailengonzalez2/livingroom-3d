export default defineAppConfig({
  ui: {
    colors: {
      // `amber` ya coincide con la luz cálida de la lámpara del loft.
      primary: 'amber',
      // Escala definida en `assets/css/main.css`, derivada del cielo.
      neutral: 'dusk'
    }
  }
})
