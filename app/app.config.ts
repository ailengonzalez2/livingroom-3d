export default defineAppConfig({
  ui: {
    colors: {
      // Ambas escalas se definen en `assets/css/main.css`, derivadas del cielo
      // del atardecer: `twilight` saturado como acento, `dusk` desaturado como
      // neutro.
      primary: 'twilight',
      neutral: 'dusk'
    }
  }
})
